import PropTypes from 'prop-types';

/**
 * The documentation shape
 */
export default {
  id: PropTypes.number,
  provider: PropTypes.string,
  providerId: PropTypes.number,
  name: PropTypes.string,
  description: PropTypes.string,
  slug: PropTypes.string,
};
